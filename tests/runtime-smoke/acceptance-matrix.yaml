# Runtime skill acceptance matrix.
#
# This file intentionally uses a small YAML subset so
# tests/runtime-smoke/run.sh can validate it with portable shell tools before
# later sprints add richer case runners.
schema_version: 1

required_fields:
  - id
  - product
  - domain
  - skill_id
  - mode
  - fixture_workspace
  - setup
  - invocation
  - expected_exit_code
  - expected_artifacts
  - cleanup
  - expected_disposition
  - skip_policy

dispositions:
  - pass
  - fail
  - skip-host-capability
  - blocked-design

cases:
  - id: meta.agent-docs.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.agent-docs
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: none
    invocation: agent-docs preflight project-dev intent in strict mode
    expected_exit_code: 0
    expected_artifacts: preflight output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.agent-out.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.agent-out
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp output root
    invocation: agent-out project allocation plus digest-gated cleanup plan/apply in temp output root
    expected_exit_code: 0
    expected_artifacts: temp output path, cleanup plan/apply JSON, bad-digest rejection JSON, and retained-marker skip
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.agent-scope-lock.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.agent-scope-lock
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create fixture-local lock root
    invocation: agent-scope-lock create and validate fixture-local lock
    expected_exit_code: 0
    expected_artifacts: fixture-local lock record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.heuristic-inbox.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.heuristic-inbox
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: resolve shared heuristic-system root and migrated retained records
    invocation: heuristic-inbox list shared root and strict-verify retained records
    expected_exit_code: 0
    expected_artifacts: shared root list and strict verification reports
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.heuristic-session-closeout.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.heuristic-session-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: inspect session-closeout skill surface and shared root
    invocation: verify session goal achieved trigger and retained-record commit boundaries
    expected_exit_code: 0
    expected_artifacts: source contract and shared-root verification guidance
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.create-skill.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.create-skill
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/fixtures/skill-lifecycle/create-skill
    setup: use repo-owned skill lifecycle fixture
    invocation: verify create-skill source, manifests, sandbox pins, and governance fixture audit
    expected_exit_code: 0
    expected_artifacts: rendered skill files and governance fixture report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.create-project-skill.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.create-project-skill
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/fixtures/skill-lifecycle/create-project-skill
    setup: use project-local skill lifecycle fixture
    invocation: verify create-project-skill source, helper modes, removed Claude-only flags, project-local fixture shape, and governance fixture audit
    expected_exit_code: 0
    expected_artifacts: project-local skill files, helper mode outputs, and governance fixture report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.remove-skill.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.remove-skill
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/fixtures/skill-lifecycle/remove-skill
    setup: use repo-owned skill lifecycle fixture
    invocation: verify remove-skill source, dry-run reference classes, and governance fixture audit
    expected_exit_code: 0
    expected_artifacts: rendered skill files and governance fixture report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.remove-project-skill.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.remove-project-skill
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/fixtures/skill-lifecycle/remove-project-skill
    setup: use project-local skill removal fixture
    invocation: verify remove-project-skill source, dry-run reference classes, and governance fixture audit
    expected_exit_code: 0
    expected_artifacts: project-local dry-run classes and governance fixture report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.repo-retro.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.repo-retro
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: enter fixture git workspace
    invocation: repo-retro dry-run or no-write report probe
    expected_exit_code: 0
    expected_artifacts: summary output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.semantic-commit.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.semantic-commit
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create fixture git workspace with staged sample change
    invocation: semantic-commit validate-only with prepared message
    expected_exit_code: 0
    expected_artifacts: validation output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: run from repo root with no-pull dry-run
    invocation: sync-runtime-surfaces planned codex refresh without runtime mutation
    expected_exit_code: 0
    expected_artifacts: planned render install doctor and summary output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.worktree-guard
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create linked git worktree from repo root
    invocation: sync-runtime-surfaces apply with linked worktree source root
    expected_exit_code: nonzero
    expected_artifacts: refusal output naming durable primary checkout
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.no-prune
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: run from repo root with no-pull dry-run and no-prune
    invocation: sync-runtime-surfaces planned codex refresh with prune skipped
    expected_exit_code: 0
    expected_artifacts: skipped prune warning and summary output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.codex-missing-cli
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: source sync-runtime-surfaces in library mode with Codex selected and no codex binary on PATH
    invocation: sync-runtime-surfaces Codex plugin registry apply helper
    expected_exit_code: nonzero
    expected_artifacts: actionable Codex CLI 0.141.0+ requirement message and no skipped activation
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.prune-fixture
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp Codex and Claude live homes with stale owned and user-owned surfaces
    invocation: agent-runtime prune-stale applies to temp live homes
    expected_exit_code: 0
    expected_artifacts: stale owned symlinks removed while foreign symlinks and regular files remain
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.sync-runtime-surfaces.shared-cli.claude-plugin-registry
    product: shared-cli
    domain: meta
    skill_id: meta.sync-runtime-surfaces
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create a temp Claude source tree/state home with a claude-kit marketplace and stub claude CLI
    invocation: sync-runtime-surfaces Claude activation helper materializes the marketplace, refreshes installed plugins, and installs every plugin ref
    expected_exit_code: 0
    expected_artifacts: symlink-free materialized marketplace plus stub log with marketplace remove/add, uninstall refresh, and per-plugin install
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.bootstrap.shared-cli.project-local
    product: shared-cli
    domain: meta
    skill_id: meta.bootstrap
    mode: deterministic
    fixture_workspace: tests/projects/project-local-smoke
    setup: use executable project-local shim script
    invocation: run .agents/scripts/bootstrap.sh from fixture workspace
    expected_exit_code: 0
    expected_artifacts: invocation marker
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.deploy.shared-cli.project-local
    product: shared-cli
    domain: meta
    skill_id: meta.deploy
    mode: deterministic
    fixture_workspace: tests/projects/project-local-smoke
    setup: use executable project-local shim script
    invocation: run .agents/scripts/deploy.sh from fixture workspace
    expected_exit_code: 0
    expected_artifacts: invocation marker
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.pre-pr.shared-cli.project-local
    product: shared-cli
    domain: meta
    skill_id: meta.pre-pr
    mode: deterministic
    fixture_workspace: tests/projects/project-local-smoke
    setup: use executable project-local shim script
    invocation: run .agents/scripts/pre-pr.sh from fixture workspace
    expected_exit_code: 0
    expected_artifacts: invocation marker
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.release.shared-cli.project-local
    product: shared-cli
    domain: meta
    skill_id: meta.release
    mode: deterministic
    fixture_workspace: tests/projects/project-local-smoke
    setup: use executable project-local shim script
    invocation: run .agents/scripts/release.sh from fixture workspace
    expected_exit_code: 0
    expected_artifacts: invocation marker
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.setup-project.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.setup-project
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/fixtures/skill-lifecycle
    setup: create temporary unadopted, partial, and apply-mode git repos
    invocation: setup-project dry-run and apply probes
    expected_exit_code: 0
    expected_artifacts: adoption diagnostics and generated pre-pr script
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.plan-archive-migrate.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.plan-archive-migrate
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic working repo with a closed plan folder and a synthetic archive clone with config/hosts.yaml
    invocation: plan-archive migrate dry-run JSON probe against the seeded plan folder
    expected_exit_code: 0
    expected_artifacts: dry-run report with archive target, file list, classification, and metadata payload
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.plan-archive-query.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.plan-archive-query
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic archive clone with a seeded _index/ snapshot tree
    invocation: plan-archive query single-ref and aggregate JSON probes against the seeded index
    expected_exit_code: 0
    expected_artifacts: snapshot records with fetched_at surfaced
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.plan-archive-discover.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.plan-archive-discover
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic working repo with one plan folder lacking provider refs and a synthetic archive clone with config/hosts.yaml
    invocation: plan-archive discover read-only JSON probe against the seeded plan folder
    expected_exit_code: 0
    expected_artifacts: discover envelope with summary counts and one blocked candidate carrying the no-provider-refs reason
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.evidence-migrate.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.evidence-migrate
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic agent-out tree with one valid and one malformed skill-usage record under an owner__repo dir, plus a synthetic single-host archive clone with config/hosts.yaml
    invocation: evidence migrate dry-run JSON probe against the seeded agent-out tree
    expected_exit_code: 0
    expected_artifacts: dry-run report resolving the valid record to an evidence/<host>/<org>/<repo> archive target and reporting the malformed record as blocked without aborting
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.evidence-prune-source.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.evidence-prune-source
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic agent-out tree with one archived and one unarchived skill-usage record, plus a synthetic archive catalog containing only the archived source digest
    invocation: evidence prune-source --archived-only dry-run JSON probe against the seeded agent-out tree
    expected_exit_code: 0
    expected_artifacts: dry-run report marking the archived source run prunable, retaining the unarchived run, and deleting nothing
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.nils-cli-bump.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.nils-cli-bump
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: write fixture pin manifests with a deliberately drifted pinned_tag and a host-aligned pinned_tag
    invocation: agent-runtime doctor --class version-alignment probe against the drift and aligned pin fixtures
    expected_exit_code: 0
    expected_artifacts: version-alignment envelope blocking on the drift fixture (exit 2) and passing on the host-aligned fixture (exit 0)
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.worktree-triage.shared-cli.deterministic
    product: shared-cli
    domain: meta
    skill_id: meta.worktree-triage
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: seed a synthetic git repo with a synthetic base ref and three linked worktrees (a fully-merged branch, a patch-equivalent branch, and a branch carrying a unique commit)
    invocation: worktree_triage.py read-only JSON scan classifying each worktree against the synthetic base ref
    expected_exit_code: 0
    expected_artifacts: worktree-triage.scan.v1 envelope classifying safe-merged, safe-superseded, and rescue-candidate dispositions
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: media.image-processing.shared-cli.deterministic
    product: shared-cli
    domain: media
    skill_id: media.image-processing
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create tiny local image fixture
    invocation: image-processing validate or convert local fixture
    expected_exit_code: 0
    expected_artifacts: converted image or validation report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: media.screen-record.shared-cli.host-capability
    product: shared-cli
    domain: media
    skill_id: media.screen-record
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: probe host screen capture capability
    invocation: screen-record capability or dry-run probe
    expected_exit_code: 0
    expected_artifacts: capability result
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip when host screen capture permission or display is unavailable
  - id: computer-use.macos-desktop.shared-cli.deterministic
    product: shared-cli
    domain: computer-use
    skill_id: computer-use.macos-desktop
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create synthetic macos-agent and SSH transports with isolated local and remote homes
    invocation: run local preflight, remote AX discovery, remote screenshot transfer, remote scenario, and unsafe-host rejection
    expected_exit_code: 0
    expected_artifacts: structured envelopes, session JSONL, pending permission actions, screenshot, and scenario result
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: browser.browser-session.shared-cli.deterministic
    product: shared-cli
    domain: browser
    skill_id: browser.browser-session
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp browser-session root
    invocation: browser-session record local session metadata
    expected_exit_code: 0
    expected_artifacts: session record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: browser.canary-check.shared-cli.deterministic
    product: shared-cli
    domain: browser
    skill_id: browser.canary-check
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp canary output root
    invocation: canary-check record passing and controlled failing commands
    expected_exit_code: 0
    expected_artifacts: canary records
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.actionable-advice.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.actionable-advice
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify prompt-style skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.actionable-knowledge.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.actionable-knowledge
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify prompt-style skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.discussion-to-implementation-doc.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.discussion-to-implementation-doc
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify workflow skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.guided-feature-build.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.guided-feature-build
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify workflow skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.handoff-session-prompt.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.handoff-session-prompt
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify workflow skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.orchestrator-first.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.orchestrator-first
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify prompt-style skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: conversation.parallel-first.shared-cli.deterministic
    product: shared-cli
    domain: conversation
    skill_id: conversation.parallel-first
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render current product surfaces
    invocation: verify prompt-style skill source and rendered Codex/Claude SKILL.md files
    expected_exit_code: 0
    expected_artifacts: rendered skill files
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.web-evidence.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.web-evidence
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create local file or localhost fixture
    invocation: web-evidence capture redacted static local evidence
    expected_exit_code: 0
    expected_artifacts: evidence bundle
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.test-first-evidence.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.test-first-evidence
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp evidence root
    invocation: test-first-evidence record failing and final validation notes
    expected_exit_code: 0
    expected_artifacts: evidence record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.review-evidence.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.review-evidence
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp evidence root
    invocation: review-evidence record final validation
    expected_exit_code: 0
    expected_artifacts: review evidence record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.skill-usage.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.skill-usage
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp skill usage root
    invocation: skill-usage record and verify invocation
    expected_exit_code: 0
    expected_artifacts: skill usage record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.docs-impact.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.docs-impact
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create controlled fixture diff
    invocation: docs-impact classify fixture diff
    expected_exit_code: 0
    expected_artifacts: docs-impact record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: evidence.model-cross-check.shared-cli.deterministic
    product: shared-cli
    domain: evidence
    skill_id: evidence.model-cross-check
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp model cross-check root
    invocation: model-cross-check record provider-boundary metadata
    expected_exit_code: 0
    expected_artifacts: cross-check record
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: reporting.daily-brief.shared-cli.regression
    product: shared-cli
    domain: reporting
    skill_id: reporting.daily-brief
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp brief root
    invocation: daily-brief offline or sample-mode helper probe
    expected_exit_code: 0
    expected_artifacts: brief summary
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: reporting.project-retro.shared-cli.regression
    product: shared-cli
    domain: reporting
    skill_id: reporting.project-retro
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: enter fixture git workspace
    invocation: repo-retro dry-run or sample-mode helper probe
    expected_exit_code: 0
    expected_artifacts: retro summary
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: reporting.topic-radar.shared-cli.regression
    product: shared-cli
    domain: reporting
    skill_id: reporting.topic-radar
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp radar root
    invocation: topic-radar offline or sample-mode helper probe
    expected_exit_code: 0
    expected_artifacts: radar summary
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: reporting.topic-radar-security.shared-cli.regression
    product: shared-cli
    domain: reporting
    skill_id: reporting.topic-radar
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: start local HTTP fixtures
    invocation: topic-radar security hardening regression tests
    expected_exit_code: 0
    expected_artifacts: oversized body, unsafe XML, and redirect rejections
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: pr.create-pr.shared-cli.deterministic
    product: shared-cli
    domain: pr
    skill_id: pr.create-pr
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git workspace with pushed upstream branch metadata
    invocation: forge-cli github/gitlab pr create dry-run validates body, branch, and taxonomy labels
    expected_exit_code: 0
    expected_artifacts: forge-cli dry-run provider command with selected labels
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: pr.create-dispatch-lane-pr.shared-cli.deterministic
    product: shared-cli
    domain: pr
    skill_id: pr.create-dispatch-lane-pr
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git workspace with pushed upstream branch metadata
    invocation: forge-cli github pr create dry-run with taxonomy labels plus dispatch session comment render
    expected_exit_code: 0
    expected_artifacts: forge-cli dry-run provider command with selected labels, dispatch session comment render, and issue comment dry-run
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: pr.close-pr.shared-cli.deterministic
    product: shared-cli
    domain: pr
    skill_id: pr.close-pr
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: none
    invocation: forge-cli github/gitlab pr checks ready merge close dry-run surfaces plus optional review-specialists scope
    expected_exit_code: 0
    expected_artifacts: forge-cli dry-run provider commands and specialist scope JSON
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: pr.deliver-pr.shared-cli.deterministic
    product: shared-cli
    domain: pr
    skill_id: pr.deliver-pr
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git workspace with pushed upstream branch metadata
    invocation: forge-cli github/gitlab pr deliver dry-run with taxonomy labels plus mandatory review-specialists scope and GitHub specialist review thread-file dry-run
    expected_exit_code: 0
    expected_artifacts: forge-cli deliver dry-run provider command with selected labels, specialist scope JSON, and PR review outcome plan with planned review threads
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: pr.review-thread-cleanup.shared-cli.deterministic
    product: shared-cli
    domain: pr
    skill_id: pr.review-thread-cleanup
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git workspaces with pushed upstream branch metadata
    invocation: forge-cli github pr review-threads resolve/reply offline dry-runs, gitlab resolve provider_unsupported, and shared skill surface assertions (list dry-run is not probed; it issues a live gh call in v1.9.1 — tracked in sympoies/nils-cli#887)
    expected_exit_code: 0
    expected_artifacts: forge-cli review-threads resolve/reply dry-run plans and the GitLab fail-closed envelope
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: issue.issue-follow-up.shared-cli.deterministic
    product: shared-cli
    domain: issue
    skill_id: issue.issue-follow-up
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create issue body and follow-up comment fixtures
    invocation: forge-cli issue create/view/comment dry-run surfaces with taxonomy labels
    expected_exit_code: 0
    expected_artifacts: forge-cli issue dry-run provider commands with selected labels
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: issue.issue-triage.shared-cli.deterministic
    product: shared-cli
    domain: issue
    skill_id: issue.issue-triage
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: none
    invocation: forge-cli inbox issue list/next/status and issue view dry-run surfaces
    expected_exit_code: 0
    expected_artifacts: forge-cli inbox and issue view dry-run provider commands
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: issue.report-plan-issue-finding.shared-cli.deterministic
    product: shared-cli
    domain: issue
    skill_id: issue.report-plan-issue-finding
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: none
    invocation: forge-cli issue list dedup + issue create dry-run with plan-issue-finding taxonomy labels
    expected_exit_code: 0
    expected_artifacts: forge-cli issue list and issue create dry-run provider commands with plan-issue-finding labels
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: code-review.code-review-focused-lens.shared-cli.deterministic
    product: shared-cli
    domain: code-review
    skill_id: code-review.code-review-focused-lens
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git diff and force selected review lenses
    invocation: review-specialists scope with testing and api-contract forced
    expected_exit_code: 0
    expected_artifacts: scope JSON with forced specialist list
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: code-review.code-review-follow-up.shared-cli.deterministic
    product: shared-cli
    domain: code-review
    skill_id: code-review.code-review-follow-up
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git diff and previous finding JSONL
    invocation: review-specialists validate and scoped rerun for affected lenses
    expected_exit_code: 0
    expected_artifacts: validation JSON and rerun scope JSON
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: code-review.code-review-pre-merge-gate.shared-cli.deterministic
    product: shared-cli
    domain: code-review
    skill_id: code-review.code-review-pre-merge-gate
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git diff and mandatory gate lenses
    invocation: review-specialists scope with testing and maintainability forced
    expected_exit_code: 0
    expected_artifacts: scope JSON with mandatory forced specialists
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: code-review.code-review-quick-pass.shared-cli.deterministic
    product: shared-cli
    domain: code-review
    skill_id: code-review.code-review-quick-pass
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git diff
    invocation: review-specialists scope for lightweight diff sizing
    expected_exit_code: 0
    expected_artifacts: scope JSON with suggested specialists
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: code-review.code-review-specialists.shared-cli.deterministic
    product: shared-cli
    domain: code-review
    skill_id: code-review.code-review-specialists
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp git diff and specialist finding JSONL
    invocation: review-specialists scope, validate, merge, and render
    expected_exit_code: 0
    expected_artifacts: scope JSON, merged JSON, and rendered report
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.create-plan-tracking-issue.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.create-plan-tracking-issue
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp plan bundle and tracking lifecycle record output
    invocation: plan-tooling validate plus plan-issue record open/post/repair-dashboard/audit with visible Task Ledger assertion
    expected_exit_code: 0
    expected_artifacts: tracking open JSON, dashboard repair JSON, post JSON with visible Task Ledger, and audit JSON
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.deliver-dispatch-plan.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.deliver-dispatch-plan
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: repair shared dispatch dashboard and post lifecycle comments
    invocation: plan-tooling validate/split-prs plus plan-issue record post/repair-dashboard/audit and specialist scope probes
    expected_exit_code: 0
    expected_artifacts: dispatch dashboard repair JSON, split JSON, audit JSON, and specialist scope JSON
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.dispatch-plan-closeout.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.dispatch-plan-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: build shared dispatch close fixture
    invocation: plan-issue record close with deterministic fixture and visible closeout evidence assertion
    expected_exit_code: 0
    expected_artifacts: dispatch close JSON with visible closeout preview and final dashboard
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.plan-tracking-issue-closeout.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: build lightweight closeout fixture
    invocation: plan-issue record close with deterministic fixture and visible closeout evidence assertion
    expected_exit_code: 0
    expected_artifacts: tracking close JSON with visible closeout preview and final dashboard
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.plan-issue-session-closeout-gate.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: build lightweight closeout fixture with source, plan, complete state, validation, review, merged PR, and no role=session comment
    invocation: plan-issue record close must block with session-missing when the installed nils-cli supports the session gate
    expected_exit_code: nonzero when supported
    expected_artifacts: close JSON or stderr showing session-missing; host-capability skip on older released binaries
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: older plan-issue releases do not yet enforce session-missing
  - id: dispatch.plan-tracking-closeout-gate.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: rewrite the canonical tracking comments fixture to remove the role=review comment and downgrade the role=state payload from status=complete to status=in-progress
    invocation: plan-issue tracking close-ready --expect-visible against the rewritten body/comments fixture
    expected_exit_code: 0
    expected_artifacts: close-ready JSON envelope with ready=false and blockers carrying both review-missing and state_complete-missing codes
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.plan-tracking-closeout-gate-happy-path.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: start from the same fixture as plan-tracking-closeout-gate (role=review missing, role=state status=in-progress) plus a minimal run-state with phase=ready_for_close and review decision, lenses, and outcome evidence
    invocation: plan-issue tracking checkpoint --live --fixture --profile tracking --post state,review --repair-dashboard followed by tracking close-ready --expect-visible against the merged comments fixture
    expected_exit_code: 0
    expected_artifacts: checkpoint JSON with mode=fixture, posted entries carrying synthesized fixture URLs for state and review, and a follow-up close-ready JSON envelope with ready=true and blockers empty
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.plan-tracking-closeout-gate-ledger-pending.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: build a plan bundle fixture whose demo-execution-state.md carries a `## Task Ledger` table with two rows at `pending`/`in-progress` plus one `done` row, alongside the visible-complete tracking comments fixture and a minimal run-state with phase=ready_for_close and bundle pointing at the fixture
    invocation: plan-issue tracking close-ready --expect-visible against the fixture body/comments with the run-state's `bundle` field set
    expected_exit_code: 0
    expected_artifacts: close-ready JSON envelope with ready=false and at least two `ledger-rows-pending` blocker entries carrying task_id 1.1 and 1.2 — proving the v0.25.7 blocker fires one entry per stuck row
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.plan-tracking-closeout-gate-ledger-clean.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.plan-tracking-issue-closeout
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: same scaffold as plan-tracking-closeout-gate-ledger-pending but every Task Ledger row is `done` with non-empty Evidence cells, proving the blocker stays silent when the ledger matches a finished plan
    invocation: plan-issue tracking close-ready --expect-visible against the clean ledger fixture with the run-state's `bundle` field set
    expected_exit_code: 0
    expected_artifacts: close-ready JSON envelope with ready=true and blockers empty, with `ledger-rows-pending` absent from the output
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.execute-plan-tracking-issue.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.execute-plan-tracking-issue
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: validate temp plan and render tracking issue lifecycle state
    invocation: plan-tooling validate plus plan-issue record audit plus visible lifecycle evidence and forge-cli pr view dry-run
    expected_exit_code: 0
    expected_artifacts: plan validation, tracking audit JSON, visible state comment, and forge-cli PR view plan
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.deliver-plan-tracking-issue.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.deliver-plan-tracking-issue
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render tracking record and review evidence record
    invocation: review-specialists scope plus review-evidence verify plus forge-cli pr checks dry-run, specialist testing/maintainability native review dry-runs, final review dry-run with issue mirror, and tracking validation comment visible render
    expected_exit_code: 0
    expected_artifacts: specialist scope JSON, review evidence JSON, provider checks, specialist PR review outcome plans, final review outcome plan, and validation comment visible render
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.review-dispatch-lane-pr.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.review-dispatch-lane-pr
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: render review evidence and shared dispatch issue body
    invocation: review-specialists scope plus review-evidence verify plus specialist forge-cli pr review dry-run with thread-file, final review dry-run with issue mirror, and dispatch review comment render
    expected_exit_code: 0
    expected_artifacts: specialist scope JSON, specialist PR review outcome plan with planned review threads, final review outcome plan, and dispatch review comment render
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: dispatch.execute-dispatch-lane.shared-cli.deterministic
    product: shared-cli
    domain: dispatch
    skill_id: dispatch.execute-dispatch-lane
    mode: deterministic
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: create temp assigned git workspace with pushed upstream branch metadata
    invocation: forge-cli pr create dry-run with taxonomy labels plus dispatch session comment render
    expected_exit_code: 0
    expected_artifacts: forge-cli PR create plan with selected labels and dispatch session comment render
    cleanup: temp-output-only
    expected_disposition: pass
    skip_policy: none
  - id: meta.agent-docs.codex.product-prompt
    product: codex
    domain: meta
    skill_id: meta.agent-docs
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp codex runtime home
    invocation: codex prompt asks for agent-docs runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: meta.agent-out.codex.product-prompt
    product: codex
    domain: meta
    skill_id: meta.agent-out
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp codex runtime home
    invocation: codex prompt asks for agent-out runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: browser.canary-check.codex.product-prompt
    product: codex
    domain: browser
    skill_id: browser.canary-check
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp codex runtime home
    invocation: codex prompt asks for canary-check runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: evidence.skill-usage.codex.product-prompt
    product: codex
    domain: evidence
    skill_id: evidence.skill-usage
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp codex runtime home
    invocation: codex prompt asks for skill-usage runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: evidence.docs-impact.codex.product-prompt
    product: codex
    domain: evidence
    skill_id: evidence.docs-impact
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp codex runtime home
    invocation: codex prompt asks for docs-impact runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: meta.agent-docs.claude.product-prompt
    product: claude
    domain: meta
    skill_id: meta.agent-docs
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp claude runtime home
    invocation: claude prompt asks for agent-docs runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: meta.agent-out.claude.product-prompt
    product: claude
    domain: meta
    skill_id: meta.agent-out
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp claude runtime home
    invocation: claude prompt asks for agent-out runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: browser.canary-check.claude.product-prompt
    product: claude
    domain: browser
    skill_id: browser.canary-check
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp claude runtime home
    invocation: claude prompt asks for canary-check runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: evidence.skill-usage.claude.product-prompt
    product: claude
    domain: evidence
    skill_id: evidence.skill-usage
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp claude runtime home
    invocation: claude prompt asks for skill-usage runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
  - id: evidence.docs-impact.claude.product-prompt
    product: claude
    domain: evidence
    skill_id: evidence.docs-impact
    mode: product
    fixture_workspace: tests/runtime-smoke/workspaces/basic-repo
    setup: install temp claude runtime home
    invocation: claude prompt asks for docs-impact runtime-smoke marker
    expected_exit_code: 0
    expected_artifacts: product prompt output or skip reason
    cleanup: temp-output-only
    expected_disposition: skip-host-capability
    skip_policy: skip unless isolated provider/auth prompt execution is explicitly enabled
