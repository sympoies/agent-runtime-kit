# SUPPORT_MATRIX surface registry.
#
# Schema: ../core/docs/schemas/surfaces.schema.json
# Architecture: docs/source/harness-shape-codex.md, docs/source/harness-shape-claude.md
# Plan: docs/plans/support-matrix-rendered/
#
# This manifest is the canonical row registry for the cross-product
# SUPPORT_MATRIX view rendered by `agent-runtime render --target
# support-matrix`.
#
# `source_manifest` provenance uses `path` or `path#anchor` (never line
# numbers); `scripts/ci/validate-surfaces-manifest.sh` resolves each entry
# so it cannot rot into a dead link.
schema_version: 1

render:
  target: support-matrix
  root_view: SUPPORT_MATRIX.md
  output: build/shared/SUPPORT_MATRIX.md

surfaces:
  - id: home-prompt
    ordinal: 1
    name: "home prompt (`AGENTS.md` / `CLAUDE.md`)"
    products:
      codex:
        state: shipped
        mechanism: "`agent-runtime render --target home-prompt --product codex` writes `build/codex/AGENT_HOME.md`; `scripts/setup.sh` and `sync-runtime-surfaces.sh --apply --product codex` wire `$CODEX_HOME/AGENTS.md` to that rendered file"
        source_artifacts:
          - AGENT_HOME.md
          - build/codex/AGENT_HOME.md
          - scripts/setup.sh
          - scripts/sync-runtime-surfaces.sh
        min_product: "0.130.0"
        min_nils_cli: "v1.12.1"
        acceptance:
          - kind: ci
            note: "— (home-policy cutover only)"
            descriptive_only: true
          - kind: live
            command: 'test -e "${CODEX_HOME:-$HOME/.codex}/AGENTS.md"'
            success:
              exit_status: 0
        source_manifest:
          - "docs/source/harness-shape-codex.md#1-home-scope-prompt-agentsmd"
          - AGENT_HOME.md
      claude:
        state: shipped
        mechanism: "`agent-runtime render --target home-prompt --product claude` writes `build/claude/AGENT_HOME.md`; `scripts/setup.sh` and `sync-runtime-surfaces.sh --apply --product claude` wire `$HOME/.claude/CLAUDE.md` to that rendered file"
        source_artifacts:
          - AGENT_HOME.md
          - build/claude/AGENT_HOME.md
          - scripts/setup.sh
          - scripts/sync-runtime-surfaces.sh
        min_product: "2.1.145"
        min_nils_cli: "v1.12.1"
        acceptance:
          - kind: ci
            note: "— (home-policy cutover only)"
            descriptive_only: true
          - kind: live
            note: "live Claude session"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#1-home-scope-prompt-claudemd"
          - AGENT_HOME.md

      hermes:
        state: shipped
        mechanism: "`agent-runtime render --target home-prompt --product hermes` writes `build/hermes/AGENT_HOME.md`; `sync-runtime-surfaces.sh --apply --product hermes` copies it to `~/.hermes/skills/development-policy/SKILL.md` (the user's `SOUL.md` references it and is never overwritten)"
        source_artifacts:
          - "AGENT_HOME.md"
          - "build/hermes/AGENT_HOME.md"
          - "scripts/sync-runtime-surfaces.sh"
        min_product: "0.17.0"
        min_nils_cli: "v1.20.0"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#1-home-scope-prompt-soulmd-development-policy-skill"
          - "AGENT_HOME.md"
  - id: project-prompt
    ordinal: 2
    name: "project prompt (`./AGENTS.md` / `./CLAUDE.md`)"
    products:
      codex:
        state: shipped
        mechanism: "repo working tree; `./AGENTS.md` tracked"
        source_artifacts:
          - AGENTS.md
        min_product: "0.130.0"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            command: "test -f AGENTS.md"
            success:
              exit_status: 0
          - kind: live
            note: "live Codex session in repo"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#2-project-scope-prompt-agentsmd"
          - AGENTS.md
      claude:
        state: shipped
        mechanism: "repo working tree; `./CLAUDE.md` symlinks to `./AGENTS.md`"
        source_artifacts:
          - "CLAUDE.md -> AGENTS.md"
        min_product: "2.1.145"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "live Claude session in repo"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#2-project-scope-prompt-claudemd"
          - AGENTS.md

      hermes:
        state: shipped
        mechanism: "Hermes auto-loads `AGENTS.md` or `.hermes.md` from the working directory up to the git root"
        source_artifacts:
          - "AGENTS.md"
        min_product: "0.17.0"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#2-project-scope-prompt-agentsmd-hermesmd"
          - "AGENTS.md"
  - id: plugin-manifest
    ordinal: 3
    name: "plugin manifest (`.codex-plugin` / `.claude-plugin` `plugin.json`)"
    products:
      codex:
        state: shipped
        mechanism: "`plugin-manifest-copy` per plugin into `$CODEX_HOME/plugins/<p>/.codex-plugin/plugin.json`; Codex's plugin loader reads it through the installed `codex-kit` marketplace. Codex discovers the plugin's bundled `skills/<skill>/SKILL.md` directly and ignores the manifest `skills` array (kept as audit metadata)."
        source_artifacts:
          - "targets/codex/plugins/<plugin>/.codex-plugin/plugin.json"
        min_product: "0.141.0"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 1 (governance plugin-skills audit), gate 5 (audit-drift plugin-manifest-skills), gate 7 (sandbox install rehearsal)"
            descriptive_only: true
          - kind: live
            note: "`codex plugin add <plugin>@codex-kit` caches `.codex-plugin/plugin.json`; `codex plugin list` shows the installed plugin"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#3-plugin-manifest-codex-pluginpluginjson"
          - manifests/product-capabilities.yaml
          - targets/codex/link-map.yaml
      claude:
        state: shipped
        mechanism: "`plugin-manifest-copy` per plugin into `~/.claude/plugins/<p>/.claude-plugin/plugin.json`"
        source_artifacts:
          - "targets/claude/plugins/<plugin>/.claude-plugin/plugin.json"
        min_product: "2.1.145"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 5 (audit-drift), gate 7 (sandbox install rehearsal)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#3-plugin-manifest-claude-pluginpluginjson"
          - manifests/product-capabilities.yaml
          - targets/claude/link-map.yaml

      hermes:
        state: shipped
        mechanism: "`plugin-manifest-copy` per plugin into `~/.hermes/plugins/<domain>/.hermes-plugin/plugin.json`; Hermes discovery is filesystem-based and does not load plugin.json at runtime"
        source_artifacts:
          - "targets/hermes/plugins/<domain>/.hermes-plugin/plugin.json"
        min_product: "0.17.0"
        min_nils_cli: "v1.20.0"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#3-plugin-manifest-hermes-pluginpluginjson"
          - "targets/hermes/link-map.yaml"
  - id: plugin-marketplace
    ordinal: 4
    name: "plugin marketplace (`.claude-plugin/marketplace.json`)"
    products:
      codex:
        state: shipped
        mechanism: "`plugin-manifest-copy` of the canonical `.agents/plugins/marketplace.json`, then `sync-runtime-surfaces.sh --apply --product codex` materializes a symlink-free marketplace under the Codex state home, registers that path as `codex-kit` (`codex plugin marketplace add`), and installs every `<plugin>@codex-kit` (`codex plugin add`). First-time `setup.sh` delegates the same activation after bootstrap."
        source_artifacts:
          - targets/codex/.agents/plugins/marketplace.json
          - scripts/sync-runtime-surfaces.sh
        min_product: "0.141.0"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 8 (runtime-smoke codex plugin-registry probes: default dry-run prints the activation plan without executing it; stubbed apply registers codex-kit and installs each <plugin>@codex-kit)"
            descriptive_only: true
          - kind: live
            note: "`sync-runtime-surfaces.sh --apply --product codex` then `codex plugin list` includes `<plugin>@codex-kit`; `codex debug prompt-input` lists skills as `<plugin>:<skill>`"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#4-plugin-marketplace-agentspluginsmarketplacejson"
          - manifests/product-capabilities.yaml
          - targets/codex/link-map.yaml
      claude:
        state: shipped
        mechanism: "`plugin-manifest-copy` of root marketplace.json, then `sync-runtime-surfaces.sh --apply --product claude` materializes a symlink-free marketplace under Claude state home, registers that path as `claude-kit`, and reinstalls every `<plugin>@claude-kit` through the Claude plugin CLI; first-time `setup.sh` delegates the same activation after bootstrap"
        source_artifacts:
          - targets/claude/.claude-plugin/marketplace.json
          - scripts/sync-runtime-surfaces.sh
          - scripts/setup.sh
        min_product: "2.1.145"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 5 (audit-drift), gate 7, gate 8 (runtime-smoke Claude materialized plugin registry and setup activation fixtures)"
            descriptive_only: true
          - kind: live
            note: "`claude plugins list` includes the runtime-kit `<plugin>@claude-kit` entries after sync, with cache populated from the state-home materialized marketplace"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#4-plugin-marketplace-claude-pluginmarketplacejson"
          - manifests/product-capabilities.yaml
          - targets/claude/link-map.yaml

      hermes:
        state: not-applicable
        mechanism: "Hermes has no plugin marketplace concept; skills are standalone SKILL.md files"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#4-plugin-marketplace"
  - id: plugin-scoped-skill-discovery
    ordinal: 5
    name: "plugin-scoped skill discovery (`plugins/<p>/skills/<s>/`)"
    products:
      codex:
        state: shipped
        mechanism: "rendered to `build/codex/plugins/<p>/skills/<s>/`, copied symlink-free into the materialized `codex-kit` marketplace; once a plugin is installed, Codex discovers each bundled `skills/<skill>/SKILL.md` as `<plugin>:<skill>`. The retired flat skill root (surface 15) is no longer installed."
        source_artifacts:
          - "core/skills/<domain>/<skill>/ -> build/codex/plugins/<p>/skills/<s>/"
          - scripts/sync-runtime-surfaces.sh
        min_product: "0.141.0"
        min_nils_cli: "v0.20.0"
        acceptance:
          - kind: ci
            note: "gate 3 (render), gate 4 (golden), gate 5 (drift), gate 8 (runtime-smoke codex plugin-registry probe)"
            descriptive_only: true
          - kind: live
            note: "`codex debug prompt-input` lists `<plugin>:<skill>` skills after default activation"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#5-plugin-scoped-skills-pluginskillsskillskillmd"
          - manifests/product-capabilities.yaml
          - targets/codex/link-map.yaml
      claude:
        state: shipped
        mechanism: "rendered to `build/claude/plugins/<p>/skills/<s>/`, then `symlinked-file` `recursive: true` per plugin"
        source_artifacts:
          - "core/skills/<domain>/<skill>/ -> build/claude/plugins/<p>/skills/<s>/"
        min_product: "2.1.145"
        min_nils_cli: "v0.20.0"
        acceptance:
          - kind: ci
            note: "gate 3 (render), gate 4 (golden), gate 5 (drift), gate 7 (sandbox), gate 8 (runtime-smoke)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#5-plugin-scoped-skills-pluginskillsskillskillmd"
          - manifests/product-capabilities.yaml
          - targets/claude/link-map.yaml

      hermes:
        state: not-applicable
        mechanism: "Hermes discovers skills from filesystem skill roots, not a Claude-style `plugins/<p>/skills/` tree (see the local and external skill roots surface)"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#5-plugin-scoped-skill-discovery-pluginspskillss"
  - id: slash-command-files
    ordinal: 6
    name: "slash command files (`commands/<n>.md`)"
    products:
      codex:
        state: not-applicable
        mechanism: "Codex has no `commands/` loader in the runtime-kit activation surface"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#6-slash-command-files-commandsnamemd-outside-skills"
      claude:
        state: shipped
        mechanism: "`symlinked-file` of `targets/claude/commands` directory into `~/.claude/commands`"
        source_artifacts:
          - targets/claude/commands/memory-clean.md
        min_product: "2.1.145"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 7 (sandbox install)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#6-slash-command-files-commandsnamemd-outside-skills"
          - targets/claude/link-map.yaml

      hermes:
        state: not-applicable
        mechanism: "runtime-kit ships no Hermes slash-command files; Hermes invokes skills by name against the discovered skill tree"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#6-slash-command-files-commandsnamemd"
  - id: subagent-definitions
    ordinal: 7
    name: "subagent definitions (`agents/<n>.md`)"
    products:
      codex:
        state: shipped
        mechanism: "rendered to `build/codex/agents/<name>.toml`, then `symlinked-file` `recursive: true` (`agents-tree`) into `$CODEX_HOME/agents/`; Codex discovers personal subagents at `~/.codex/agents/*.toml` (TOML name / description / developer_instructions, optional `sandbox_mode = read-only`)"
        source_artifacts:
          - "core/agents/<domain>/<name>/AGENT.md.tera -> build/codex/agents/<name>.toml"
        min_product: "0.139.0"
        min_nils_cli: "v1.3.0"
        acceptance:
          - kind: ci
            note: "gate 3 (render), gate 4 (golden), gate 5 (drift), gate 7 (sandbox install rehearsal)"
            descriptive_only: true
          - kind: live
            note: "Codex live subagent discovery is manual-only (needs an authenticated session); documented in tests/runtime-smoke/README.md"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#7-subagent-definitions-agentsnamemd"
          - manifests/agents.yaml
          - targets/codex/link-map.yaml
      claude:
        state: shipped
        mechanism: "rendered to `build/claude/agents/<name>.md`, then `symlinked-file` `recursive: true` (`agents-tree`) into `~/.claude/agents/`; Claude discovers user subagents at `~/.claude/agents/*.md` (YAML frontmatter name / description / tools + Markdown system prompt)"
        source_artifacts:
          - "core/agents/<domain>/<name>/AGENT.md.tera -> build/claude/agents/<name>.md"
        min_product: "2.1.145"
        min_nils_cli: "v1.3.0"
        acceptance:
          - kind: ci
            note: "gate 3 (render), gate 4 (golden), gate 5 (drift), gate 7 (sandbox install rehearsal)"
            descriptive_only: true
          - kind: live
            note: "Claude live subagent discovery is manual-only (needs an authenticated session); documented in tests/runtime-smoke/README.md"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#7-subagent-definitions-agentsnamemd"
          - manifests/agents.yaml
          - targets/claude/link-map.yaml

      hermes:
        state: not-applicable
        mechanism: "Hermes subagent dispatch uses the `delegate_task` tool, not file-based agent definitions"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#7-subagent-definitions-agentsnamemd"
  - id: hook-scripts
    ordinal: 8
    name: "hook scripts (`hooks/<n>.*`)"
    products:
      codex:
        state: shipped
        mechanism: "`core/hooks/shared/` symlinked into `$CODEX_HOME/hooks`"
        source_artifacts:
          - core/hooks/shared/
        min_product: "0.130.0"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 10 (`tests/hooks/run.sh`)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#8-hook-scripts-hooksname"
          - targets/codex/link-map.yaml
      claude:
        state: partial
        mechanism: "`core/hooks/shared/` symlinked into `~/.claude/hooks`; `targets/claude/hooks/` adapter slot reserved but empty"
        source_artifacts:
          - core/hooks/shared/
        min_product: "2.1.145"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 10 (`tests/hooks/run.sh`)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#8-hook-scripts-hooksname"
          - targets/claude/link-map.yaml

      hermes:
        state: not-applicable
        mechanism: "runtime-kit does not render Hermes hook scripts; Hermes manages its approval/hook configuration natively under `~/.hermes`"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#8-hook-scripts-hooksname"
  - id: hook-settings-json
    ordinal: 9
    name: "hook registration via `settings.json` block"
    products:
      codex:
        state: not-applicable
        mechanism: "Codex hook activation is TOML-only (surface 16); no `settings.json` equivalent"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#9-hook-registration-settingsjson-hooks-block"
      claude:
        state: shipped
        mechanism: "`sync-runtime-surfaces.sh --apply --product claude` merges `core/hooks/claude/settings.hooks.jsonc` into `~/.claude/settings.json`, replacing only runtime-kit managed hook commands"
        source_artifacts:
          - core/hooks/claude/settings.hooks.jsonc
          - scripts/sync-runtime-surfaces.sh
        min_product: "2.1.145"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 8 (runtime-smoke)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#9-hook-registration-settingsjson-hooks-block"
          - manifests/runtime-roots.yaml

      hermes:
        state: not-applicable
        mechanism: "`settings.json` hook registration is a Claude-specific primitive"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#9-hook-registration-settingsjson-block"
  - id: output-styles
    ordinal: 10
    name: "output styles (`output-styles/<n>.md`)"
    products:
      codex:
        state: not-applicable
        mechanism: "Codex has no output-styles loader"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#10-output-styles-output-stylesnamemd"
      claude:
        state: not-shipped
        mechanism: "runtime-kit does not ship any `output-styles/<n>.md` source today"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#10-output-styles-output-stylesnamemd"

      hermes:
        state: not-applicable
        mechanism: "output styles are a Claude-specific primitive; the kit ships none for Hermes"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#10-output-styles-output-stylesnamemd"
  - id: status-line
    ordinal: 11
    name: "status line (`statusLine` in `settings.json`)"
    products:
      codex:
        state: not-applicable
        mechanism: "Codex has no `settings.json` / statusLine loader"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#11-status-line-statusline-in-settingsjson"
      claude:
        state: not-shipped
        mechanism: "runtime-kit does not template `statusLine`; same gap as the `settings.json` slot itself"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#11-status-line-statusline-in-settingsjson"

      hermes:
        state: not-applicable
        mechanism: "the `statusLine` setting is a Claude-specific primitive"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#11-status-line"
  - id: mcp-servers
    ordinal: 12
    name: "MCP servers (per-user MCP config)"
    products:
      codex:
        state: not-shipped
        mechanism: "runtime-kit does not template MCP servers; per-user MCP credentials are classified as sensitive"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#12-mcp-servers-per-user-mcp-config"
      claude:
        state: not-shipped
        mechanism: "same as codex; out-of-scope by design (secret boundary)"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#12-mcp-servers-per-user-mcp-config"

      hermes:
        state: not-shipped
        mechanism: "Hermes supports MCP natively, but the runtime-kit ships no Hermes MCP server configuration today"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#12-mcp-servers-per-user-mcp-config"
  - id: heuristic-system
    ordinal: 13
    name: "heuristic system (curated retained records)"
    products:
      codex:
        state: shipped
        mechanism: "shared policy root; consumed by `heuristic-inbox` binary (`--inbox-dir` case mechanics, `deliver --label` records-branch PR writeback, and operation-record archival)"
        source_artifacts:
          - core/policies/heuristic-system/
        min_product: "0.130.0"
        min_nils_cli: "v1.8.0"
        acceptance:
          - kind: ci
            note: "gate 8 (runtime-smoke deterministic, meta domain)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#13-heuristic-system-curated-retained-records"
          - core/policies/heuristic-system/
      claude:
        state: shipped
        mechanism: "same shared policy root"
        source_artifacts:
          - core/policies/heuristic-system/
        min_product: "2.1.145"
        min_nils_cli: "v1.8.0"
        acceptance:
          - kind: ci
            note: "gate 8 (runtime-smoke deterministic, meta domain)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#13-heuristic-system-curated-retained-records"
          - core/policies/heuristic-system/

      hermes:
        state: shipped
        mechanism: "shared policy root; Hermes references `core/policies/heuristic-system/` through SOUL.md and the rendered skills"
        source_artifacts:
          - "core/policies/heuristic-system/"
        min_product: "0.17.0"
        min_nils_cli: "v1.20.0"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#13-heuristic-system-curated-retained-records"
          - "core/policies/heuristic-system/HEURISTIC_SYSTEM.md"
  - id: runtime-state
    ordinal: 14
    name: "runtime state (`state_home`)"
    products:
      codex:
        state: shipped
        mechanism: "`CODEX_AGENT_STATE_HOME` env var + `agent-out` runtime allocator"
        source_artifacts:
          - "manifests/runtime-roots.yaml codex block"
        min_product: "0.130.0"
        min_nils_cli: "v1.19.2"
        acceptance:
          - kind: ci
            note: "gate 5 (drift), gate 7 (doctor `block=0`)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#14-runtime-state-statehome"
          - manifests/runtime-roots.yaml
          - manifests/product-capabilities.yaml
      claude:
        state: shipped
        mechanism: "`CLAUDE_KIT_STATE_HOME` env var + `agent-out` runtime allocator"
        source_artifacts:
          - "manifests/runtime-roots.yaml claude block"
        min_product: "2.1.145"
        min_nils_cli: "v1.19.2"
        acceptance:
          - kind: ci
            note: "gate 5 (drift), gate 7 (doctor `block=0`)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#14-runtime-state-statehome"
          - manifests/runtime-roots.yaml
          - manifests/product-capabilities.yaml

      hermes:
        state: shipped
        mechanism: "`HERMES_HOME` env var or the default `~/.hermes`"
        source_artifacts:
          - "manifests/runtime-roots.yaml"
        min_product: "0.17.0"
        min_nils_cli: "v1.20.0"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#14-runtime-state-statehome"
          - "manifests/runtime-roots.yaml#hermes"
  - id: codex-local-skill-root
    ordinal: 15
    name: "Codex local skill root (`$CODEX_HOME/skills/<d>/<s>/`)"
    products:
      codex:
        state: not-applicable
        mechanism: "Retired for runtime-kit-managed Codex skills; Codex now uses plugin-scoped skill discovery through the installed `codex-kit` marketplace (surface 5)."
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#15-codex-local-skill-root-skillsdomainskillskillmd"
          - targets/codex/link-map.yaml
          - manifests/product-capabilities.yaml
      claude:
        state: not-applicable
        mechanism: "Claude uses plugin-scoped skill discovery (surface 5) instead"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#5-plugin-scoped-skills-pluginskillsskillskillmd"
          - manifests/product-capabilities.yaml

      hermes:
        state: shipped
        mechanism: "recursive `symlinked-file` copy of each plugin's rendered skills tree into `~/.hermes/external-skills/agent-runtime-kit/<domain>/<skill>/SKILL.md`; Hermes discovers it through `skills.external_dirs`, while `~/.hermes/skills` stays reserved for Hermes-native/operator-managed skills"
        source_artifacts:
          - "targets/hermes/link-map.yaml"
        min_product: "0.17.0"
        min_nils_cli: "v1.20.0"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#15-local-and-external-skill-roots"
          - "targets/hermes/link-map.yaml"
  - id: codex-hook-registration
    ordinal: 16
    name: "Codex hook registration (`config.toml` managed block)"
    products:
      codex:
        state: shipped
        mechanism: "`managed-block` sync into `$CODEX_HOME/config.toml`, surface `hooks`, hash-comment markers"
        source_artifacts:
          - targets/codex/hooks/config.block.toml
        min_product: "0.130.0"
        min_nils_cli: "v0.17.5"
        acceptance:
          - kind: ci
            note: "gate 5 (drift, managed-block presence + marker pair), gate 10 (`tests/hooks/run.sh`)"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#16-codex-hook-registration-configtoml-managed-block"
          - targets/codex/link-map.yaml
      claude:
        state: not-applicable
        mechanism: "Claude uses `settings.json` block (surface 9) instead; the two are different upstream primitives"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "—"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#9-hook-registration-settingsjson-hooks-block"
          - manifests/runtime-roots.yaml

      hermes:
        state: not-applicable
        mechanism: "the Codex `config.toml` managed block is a Codex-specific primitive"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#16-codex-hook-registration-configtoml-managed-block"
  - id: prompt-mode-delegation-policy
    ordinal: 17
    name: "prompt-mode delegation policy (`AGENT_HOME.md`)"
    products:
      codex:
        state: shipped
        mechanism: "Codex-only policy text is rendered into `build/codex/AGENT_HOME.md` and loaded through the home prompt (surface 1)"
        source_artifacts:
          - AGENT_HOME.md
          - build/codex/AGENT_HOME.md
        min_product: "0.130.0"
        min_nils_cli: "v1.12.1"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "live Codex session"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-codex.md#17-prompt-mode-delegation-policy-agenthomemd"
          - AGENT_HOME.md
      claude:
        state: not-applicable
        mechanism: "Codex-only delegation policy is omitted from the rendered Claude home prompt; Claude Code uses its native reviewer defaults"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "—"
            descriptive_only: true
          - kind: live
            note: "live Claude session"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-claude.md#1-home-scope-prompt-claudemd"
          - AGENT_HOME.md
      hermes:
        state: not-applicable
        mechanism: "Codex-only delegation policy is omitted from the rendered Hermes development-policy skill; Hermes-specific `delegate_task` guidance lives in rendered code-review skills, not in `AGENT_HOME.md`"
        source_artifacts: []
        min_product: "n/a"
        min_nils_cli: "n/a"
        acceptance:
          - kind: ci
            note: "--"
            descriptive_only: true
          - kind: live
            note: "--"
            descriptive_only: true
        source_manifest:
          - "docs/source/harness-shape-hermes.md#17-prompt-mode-delegation-policy-agenthomemd"
          - "AGENT_HOME.md"
